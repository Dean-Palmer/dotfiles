_toggl_completion() {
    local IFS=$'\t'
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _TOGGL_COMPLETE=complete-bash $1 ) )
    return 0
}

complete -F _toggl_completion -o default toggl
